{
  "name": "aikscript",
  "version": "1.0.0",
  "description": "AikScript: TypeScript-to-Aiken development copilot for Cardano smart contracts",
  "main": "dist/src/index.js",
  "bin": {
    "aikscript": "./dist/src/cli/index.js"
  },
  "scripts": {
    "build": "tsc --build",
    "build:clean": "tsc --build --clean",
    "build:watch": "tsc --build --watch",
    "build:stdlib": "tsc --build src/core/packages/aiken-lang/stdlib/v2.2.0/tsconfig.json",
    "build:fuzz": "tsc --build src/core/packages/aiken-lang/fuzz/v2.2.0/tsconfig.json",
    "build:prelude": "tsc --build src/core/packages/aiken-lang/prelude/v1.0.0/tsconfig.json",
    "build:mpf": "tsc --build src/core/packages/aiken-lang/merkle-patricia-forestry/v2.1.0/tsconfig.json",
    "build:all": "npm run build:stdlib && npm run build:fuzz && npm run build:prelude && npm run build:mpf && npm run build",
    "dev": "ts-node src/cli/index.ts",
    "watch": "nodemon --exec ts-node src/cli/index.ts",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:stdlib": "jest --testPathPattern=stdlib",
    "test:fuzz": "jest --testPathPattern=fuzz",
    "test:prelude": "jest --testPathPattern=prelude",
    "test:mpf": "jest --testPathPattern=merkle-patricia-forestry",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "lint:packages": "eslint src/core/packages/**/*.ts",
    "format": "npx prettier --write \"src/**/*.ts\" \"tests/**/*.ts\"",
    "format:check": "npx prettier --check \"src/**/*.ts\" \"tests/**/*.ts\"",
    "format:packages": "npx prettier --write \"src/core/packages/**/*.ts\"",
    "clean": "rm -rf dist && find src/core/packages -name dist -type d -exec rm -rf {} +",
    "clean:build": "npm run clean && npm run build:all",
    "prepublishOnly": "npm run clean:build && npm test",
    "validate": "npm run lint && npm run test && npm run build",
    "validate:packages": "npm run lint:packages && npm run test && npm run build:all",
    "postinstall": "echo '\\n🎉 AikScript installed successfully!\\n\\nQuick start:\\n  aikscript --help\\n  aikscript new my-project\\n\\nDocumentation: https://github.com/FractionEstate/AikScript\\n'",
    "bump": "node ../../scripts/bump-version.js"
  },
  "keywords": [
    "cardano",
    "smart-contracts",
    "aiken",
    "typescript",
    "blockchain",
    "dapp",
    "web3",
    "cryptocurrency",
    "defi",
    "nft"
  ],
  "author": "FractionEstate <https://github.com/FractionEstate>",
  "license": "MIT",
  "funding": {
    "type": "github",
    "url": "https://github.com/sponsors/FractionEstate"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "@types/jest": "^29.5.14",
    "@types/node": "^24.3.0",
    "@typescript-eslint/eslint-plugin": "^8.42.0",
    "@typescript-eslint/parser": "^8.42.0",
    "eslint": "^9.34.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "prettier": "^2.8.8",
    "ts-jest": "^29.3.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "commander": "^9.5.0",
    "fs-extra": "^10.1.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FractionEstate/AikScript.git"
  },
  "homepage": "https://github.com/FractionEstate/AikScript#readme",
  "bugs": {
    "url": "https://github.com/FractionEstate/AikScript/issues"
  },
  "engines": {
    "node": ">=20.19.5"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "preferGlobal": true
}
